<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Create a new landmark</title>
  </head>
  <body>

    <form class="" action="/landmarks/<%= @landmark.id %>" method="post">
      <input type="hidden" name="_method" value="patch" />

      <label for="">Name:</label>
      <input id="name" type="text" name="landmark[name]" value="<%= @landmark.name %>">

      <label for="">Year completed:</label>
      <input id="year_completed" type="text" name="landmark[year_completed]" value="<%= @landmark.year_completed %>">

      <label for="figure_select">Figure:</label>
      <select id="figure_select" name="landmark[figure_id]">
        <% @figures.each do |figure| %>
          <option value="<%= figure.id %>"
            <% if @landmark.figure_id == figure.id %> selected <% end %>
            ><%= figure.name %></option>
          <% end %>

      </select>
      <input id="Edit Landmark" type="submit" />
    </form>

    <form action="/landmarks/<%= @landmark.id %>" method="post">
      <input type="hidden" name="_method" value="delete" />
      <input type="submit" name="" value="Delete Landmark">
    </form>

  </body>
</html>
